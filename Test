import re
import time
from ipaddress import ip_interface, ip_network
import pandas as pd
from netmiko import ConnectHandler
import telnetlib

def get_running_config(
        host: str = "192.168.4.4",
        router: str = "",
        port: int = 0,
        username: str = "",
        password: str = "",  # По умолчанию пусто
        enable_password: str = "\n",  # Enter для enable (можно изменить)
        save_to_file: bool = True,  # Сохранять ли конфиг в файл?
        verbose: bool = True  # Выводить логи в консоль?
):
    global connection
    device = {
        "device_type": "cisco_ios_telnet",  # Важно: указываем Telnet!
        "host": host,  # Адрес PNET Lab
        "port": port,  # Telnet-порт устройства (например, R1)
        "secret": enable_password,  # Пароль enable (если есть)
        "global_delay_factor": 2  # Замедление для старых эмуляторов
    }

    try:
        # 1. Подключение
        print("🔌 Подключаюсь к устройству...")
        connection = ConnectHandler(**device)

        # 2. Отправляем "\n" (если устройство "спит")
        connection.write_channel("\n")
        time.sleep(1)

        # 3. Проверяем, есть ли enable-режим
        if device["secret"]:
            connection.enable()

        # 4. Проверяем текущий режим (возможно, уже в config mode)
        prompt = connection.find_prompt()
        if "(config)" in prompt:
            print("⚠️ Устройство уже в режиме configure terminal!")
        else:
            print("⌛ Переход в 'configure terminal'...")
            connection.config_mode()  # Пытаемся войти в conf t

        # 5. Выполняем "do show running-config"
        print("🔍 Выполняю 'do show running-config'...")
        output = connection.send_command("do show running-config", delay_factor=2)

        # 6. Сохраняем конфигурацию
        with open(f"{router}_running_config.txt", "w") as f:
            f.write(output)
        print(f"✅ Конфигурация сохранена в {router}_running_config.txt")

        # 7. Выходим из config mode (если были внутри)
        if "(config)" in connection.find_prompt():
            connection.exit_config_mode()

    except Exception as e:
        print(f"🚨 Ошибка: {e}")

    finally:
        if "connection" in locals():
            connection.disconnect()
            print("🔒 Сессия закрыта.")


def check_router_config(router: str, config: str) -> dict:
    """
    Проверяет конфигурацию роутера согласно топологии
    Возвращает словарь с результатами проверки интерфейсов
    """
    results = {}

    # Строгие проверки для конкретных интерфейсов
    strict_checks = {
        "R1": {"Ethernet0/0": "173.14.203.1/24"},
        "R3": {"Ethernet0/0": "173.14.201.1/24"},
        "R4": {"Ethernet0/2": "173.14.202.1/24"}
    }

    # Проверка строгих требований
    if router in strict_checks:
        for interface, expected_ip in strict_checks[router].items():
            results[interface] = check_exact_ip(config, interface, expected_ip)

    # Проверка остальных интерфейсов согласно топологии
    if router == "R1":
        results["Ethernet0/1"] = check_ip_in_subnet(config, "Ethernet0/1", "197.148.231.12/30")

    elif router == "R2":
        results["Ethernet0/0"] = check_ip_in_subnet(config, "Ethernet0/0", "197.148.231.0/30")
        results["Ethernet0/1"] = check_ip_in_subnet(config, "Ethernet0/1", "197.148.231.12/30")
        results["Ethernet0/2"] = check_ip_in_subnet(config, "Ethernet0/2", "197.148.231.4/30")

    elif router == "R3":
        results["Ethernet0/1"] = check_ip_in_subnet(config, "Ethernet0/1", "197.148.231.8/30")
        results["Ethernet0/2"] = check_ip_in_subnet(config, "Ethernet0/2", "197.148.231.0/30")

    elif router == "R4":
        results["Ethernet0/0"] = check_ip_in_subnet(config, "Ethernet0/0", "197.148.231.8/30")
        results["Ethernet0/1"] = check_ip_in_subnet(config, "Ethernet0/1", "197.148.231.4/30")

    return results


def check_exact_ip(config: str, interface: str, expected_ip: str) -> bool:
    """
    Проверяет точное соответствие IP на интерфейсе и его статус (shutdown)
    """
    expected = ip_interface(expected_ip)
    # Ищем блок конфигурации интерфейса
    pattern = rf"interface {interface}.*?(?=^interface|\Z)"
    match = re.search(pattern, config, re.DOTALL | re.MULTILINE)
    if not match:
        return False

    interface_block = match.group(0)
    # Проверяем наличие shutdown в блоке интерфейса
    if re.search(r"^\s+shutdown\b", interface_block, re.MULTILINE):
        return False

    # Проверяем точный IP-адрес
    ip_pattern = rf"ip address {expected.ip}\s+{expected.netmask}"
    return bool(re.search(ip_pattern, interface_block))


def check_ip_in_subnet(config: str, interface: str, subnet: str) -> bool:
    """
    Проверяет, что на интерфейсе есть любой IP из указанной подсети и интерфейс не выключен
    """
    network = ip_network(subnet, strict=False)
    # Ищем блок конфигурации интерфейса
    pattern = rf"interface {interface}.*?(?=^interface|\Z)"
    match = re.search(pattern, config, re.DOTALL | re.MULTILINE)
    if not match:
        return False

    interface_block = match.group(0)
    # Проверяем наличие shutdown в блоке интерфейса
    if re.search(r"^\s+shutdown\b", interface_block, re.MULTILINE):
        return False

    # Ищем IP-адрес в блоке интерфейса
    ip_pattern = r"ip address (\d+\.\d+\.\d+\.\d+)\s+(\d+\.\d+\.\d+\.\d+)"
    ip_match = re.search(ip_pattern, interface_block)
    if not ip_match:
        return False

    ip = ip_match.group(1)
    mask = ip_match.group(2)

    try:
        interface_ip = ip_interface(f"{ip}/{mask}")
        return interface_ip.network == network
    except ValueError:
        return False


def check_static_addresses(config: str) -> bool:
    """
    Проверяет, что нет DHCP на интерфейсах
    """
    return "ip address dhcp" not in config


def parse_pnet_ports(file_path: str) -> dict:
    """
    Получает порты всех узлов из файла PNET Lab
    Возвращает словарь вида {'R1': 30812, 'R2': 30813, ..., 'SRV': 30820}
    """
    try:
        df = pd.read_html(file_path)[0]
        df = df[df['Node Name'].notna()]
        return dict(zip(df['Node Name'], df['Port']))
    except Exception as e:
        print(f"Ошибка: {e}")
        return {}

def check_server_config(config: str) -> bool:
    """
    Проверяет, что интерфейс e0/0 на сервере SRV включен и имеет IP 173.14.231.100/24
    """
    expected = ip_interface("173.14.201.100/24")
    pattern = rf"interface Ethernet0/0.*?(?=^interface|\Z)"
    match = re.search(pattern, config, re.DOTALL | re.MULTILINE)
    if not match:
        return False

    block = match.group(0)
    if re.search(r"^\s+shutdown\b", block, re.MULTILINE):
        return False

    ip_pattern = rf"ip address {expected.ip}\s+{expected.netmask}"
    return bool(re.search(ip_pattern, block))

def check_vpc_dhcp(host: str, port: int, expected_subnet: str) -> bool:
    try:
        with telnetlib.Telnet(host, port, timeout=5) as tn:
            time.sleep(1)
            tn.write(b"\n")
            time.sleep(1)
            tn.write(b"ip dhcp\n")
            time.sleep(10)
            output = tn.read_very_eager().decode(errors="ignore")
            print(output)

        # Проверка: есть ли DORA / DDORA / DDDORA
        if not re.search(r"D+ORA", output):
            return False

        # Поиск IP
        match = re.search(r"IP\s+(\d+\.\d+\.\d+\.\d+)/\d+", output)
        if not match:
            return False

        ip = match.group(1)
        return ip_interface(f"{ip}/24").ip in ip_network(expected_subnet)

    except Exception as e:
        print(f"🚨 Ошибка подключения к VPC ({port}): {e}")
        return False

def get_vpc_ip(host: str, port: int) -> str:
    """Получает текущий IP адрес VPC через show ip all"""
    try:
        with telnetlib.Telnet(host, port, timeout=5) as tn:
            time.sleep(1)
            tn.write(b"\n")
            time.sleep(1)
            tn.write(b"show ip all\n")
            time.sleep(2)
            output = tn.read_very_eager().decode(errors="ignore")
            match = re.search(r"IP/MASK\s+([0-9.]+)", output)
            if match:
                return match.group(1)
    except Exception as e:
        print(f"❌ Ошибка при получении IP VPC ({port}): {e}")
    return None


def ping_vpc(host: str, port: int, target_ip: str) -> bool:
    """Пингует другой IP адрес из VPC"""
    try:
        with telnetlib.Telnet(host, port, timeout=5) as tn:
            time.sleep(1)
            tn.write(b"\n")
            time.sleep(1)
            command = f"ping {target_ip}\n".encode()
            tn.write(command)
            time.sleep(4)
            output = tn.read_very_eager().decode(errors="ignore")
            return "ttl=" in output.lower() or "icmp_seq=" in output.lower()
    except Exception as e:
        print(f"❌ Ошибка пинга с VPC ({port}): {e}")
    return False


def extract_ip_from_show_ip(output: str) -> str:
    """
    Парсит вывод show ip all и извлекает IP-адрес
    """
    match = re.search(r"\bIP/MASK\s+GATEWAY.*\n.*?(\d+\.\d+\.\d+\.\d+)/\d+", output)
    if match:
        return match.group(1)
    else:
        raise ValueError("❌ Не удалось найти IP в выводе команды show ip all")

def ping_test(host: str, port: int, targets: list[str]) -> dict:
    """
    Выполняет пинг с VPC-хоста до списка IP-адресов
    Возвращает словарь с результатами по каждому IP
    """
    results = {}
    try:
        with telnetlib.Telnet(host, port, timeout=5) as tn:
            time.sleep(1)
            tn.write(b"\n")
            time.sleep(1)
            tn.write(b"show ip all\n")
            time.sleep(1)
            output = tn.read_very_eager().decode(errors="ignore")
            print(f"🔎 Вывод show ip all:\n{output}")
            my_ip = extract_ip_from_show_ip(output)
            print(f"🧠 Мой IP: {my_ip}")

            for target_ip in targets:
                tn.write(f"ping {target_ip}\n".encode())
                time.sleep(5)
                ping_output = tn.read_very_eager().decode(errors="ignore")
                print(f"📡 Пинг {target_ip}:\n{ping_output}")
                if ("unreachable" in ping_output.lower() or
                    "timeout" in ping_output.lower()):
                    results[target_ip] = False
                else:
                    results[target_ip] = True
    except Exception as e:
        print(f"🚨 Ошибка при пинге с порта {port}: {e}")
        for ip in targets:
            results[ip] = False
    return results




if __name__ == '__main__':
    try:
        # 1. Получаем порты
        ports = parse_pnet_ports("node_sessions.xls")
        print("✅ Найдены порты:", ports)

        # 2. Пример использования с get_running_config
        for router, port in ports.items():
            print(f"\n🔧 Получаю конфигурацию {router} (порт {port})...")
            config = get_running_config(port=port, router=router)
            print(config)
    except ValueError as e:
        print(f"🚨 Ошибка: {e}")
    except Exception as e:
        print(f"⚠️ Неожиданная ошибка: {e}")

    print("Задание 1. Настроить IP адреса маршрутизаторов")

    for router in ["R1", "R2", "R3", "R4"]:
        try:
            with open(f"{router}_running_config.txt", "r") as f:
                config = f.read()

            checks = check_router_config(router, config)
            print(f"\n{router} проверка:")
            for interface, status in checks.items():
                print(f"{interface}: {'✅' if status else '❌'}")

        except FileNotFoundError:
            print(f"⚠️ Файл конфигурации {router} не найден")

    print("\nЗадание 2. Настроить адрес сервера")

    if "SRV" in ports:
        try:
            with open("SRV_running_config.txt", "r") as f:
                srv_config = f.read()

            if check_server_config(srv_config):
                print("SRV: ✅ Адрес настроен правильно")
            else:
                print("SRV: ❌ Ошибка в настройке интерфейса Ethernet0/0")
        except FileNotFoundError:
            print("⚠️ Файл конфигурации SRV не найден")
    else:
        print("⚠️ Узел SRV не найден в node_sessions.xls")

    print("\nЗадание 3. Проверка получения IP по DHCP на VPC-хостах")

    vpc_targets = {
        "VPC-Samara": "173.14.201.0/24",
        "VPC-Izhevsk": "173.14.202.0/24",
        "VPC-Kazan": "173.14.203.0/24"
    }

    for vpc_name, subnet in vpc_targets.items():
        if vpc_name in ports:
            print(f"\n🔄 Проверяю {vpc_name}...")
            success = check_vpc_dhcp("192.168.4.4", ports[vpc_name], subnet)
            print(f"{vpc_name}: {'✅ Получен IP из нужной подсети' if success else '❌ Ошибка DHCP или неверный IP'}")
        else:
            print(f"{vpc_name}: ⚠️ Порт не найден в node_sessions.xls")

    print("\nЗадание 4. Проверка пингов между VPC")

    ip_map = {}
    host = '192.168.4.4'
    for vpc in ["VPC-Izhevsk", "VPC-Kazan", "VPC-Samara"]:
        if vpc in ports:
            try:
                with telnetlib.Telnet(host, ports[vpc], timeout=5) as tn:
                    tn.write(b"\n")
                    time.sleep(1)
                    tn.write(b"show ip all\n")
                    time.sleep(1)
                    output = tn.read_very_eager().decode(errors="ignore")
                    ip_map[vpc] = extract_ip_from_show_ip(output)
            except Exception as e:
                print(f"⚠️ Не удалось получить IP для {vpc}: {e}")
                ip_map[vpc] = None

    # Проверка с VPC-Samara
    if ports.get("VPC-Samara"):
        print("\n✅ Проверка с VPC-Samara:")
        result = ping_test(host, ports["VPC-Samara"], [
            ip_map.get("VPC-Izhevsk"),
            ip_map.get("VPC-Kazan")
        ])
        for target, status in result.items():
            print(f"Пинг до {target}: {'✅ Успешно' if status else '❌ Ошибка'}")

    # Проверка с VPC-Izhevsk
    if ports.get("VPC-Izhevsk"):
        print("\n✅ Проверка с VPC-Izhevsk:")
        result = ping_test(host, ports["VPC-Izhevsk"], [
            ip_map.get("VPC-Kazan")
        ])
        for target, status in result.items():
            print(f"Пинг до {target}: {'✅ Успешно' if status else '❌ Ошибка'}")
